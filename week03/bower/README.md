# Intro To bower_components

Starting off using `bower`.

## Usage

Make sure `bower` has been installed globally:

```bash
$ npm instal -g bower
```

Then install `bower` packages:

```bash
$ bower install

```
